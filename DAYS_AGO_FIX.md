# ✅ إصلاح مشكلة الأرقام العشرية في days_ago

## 🐛 المشكلة

كان `days_ago` يرجع أرقام عشرية مثل:

```
1.4659708496991
```

---

## ✅ الحل

تم إضافة **Integer Casting** للتأكد من أن النتيجة دائماً رقم صحيح:

```php
private function getTimeAgo($createdAt)
{
    $diffInHours = (int) $createdAt->diffInHours();  // ✅ Integer
    $diffInDays = (int) $createdAt->diffInDays();    // ✅ Integer

    if ($diffInDays > 0) {
        return 'منذ ' . $diffInDays . ' ' . ($diffInDays == 1 ? 'يوم' : 'أيام');
    } else {
        return 'منذ ' . $diffInHours . ' ' . ($diffInHours == 1 ? 'ساعة' : 'ساعات');
    }
}
```

---

## 📊 قبل وبعد

### ❌ قبل:

```json
{
    "days_ago": "منذ 1.4659708496991 أيام"
}
```

### ✅ بعد:

```json
{
    "days_ago": "منذ 1 يوم"
}
```

---

## 🎯 الأمثلة

| Input       | Output        |
| ----------- | ------------- |
| 1.8 ساعة    | `منذ 1 ساعة`  |
| 5.999 ساعات | `منذ 5 ساعات` |
| 1.5 يوم     | `منذ 1 يوم`   |
| 2.99 يوم    | `منذ 2 أيام`  |
| 7.1 أيام    | `منذ 7 أيام`  |

---

**الآن جميع الأرقام صحيحة بدون كسور عشرية! ✅**
